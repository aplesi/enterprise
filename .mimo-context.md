# MIMO Context — Aplesi Enterprise

## Deploy Status: ✅ BERHASIL (2026-07-15)

### Deploy Configuration
- **Build**: `opennextjs-cloudflare build` (OpenNext + Next.js 16.2.10 + Turbopack)
- **Deploy**: `wrangler deploy` (Worker with assets binding — handles static + server functions)
- **Worker Name**: `aplesi-worker` (standalone Worker, NOT the Pages project `enterprise`)
- **URL Production**: https://aplesi.my.id (custom domain via Worker routes)
- **URL Preview**: https://enterprise-279.pages.dev (Pages project, static only)
- **Account**: Aplesi.co@gmail.com (Account ID: 3564a2d3d75a9c4794613b8fdeb25359)

### Architecture (IMPORTANT!)
- **Worker `aplesi-worker`** = serves everything (static assets + SSR + API routes)
  - Routes: `aplesi.my.id/*`, `www.aplesi.my.id/*`
  - Assets binding: `.open-next/assets`
  - Bindings: D1 (`aplesi-db`), KV (`aplesi_kv`), Self-reference (`aplesi-worker`)
- **Pages project `enterprise`** = preview only (enterprise-279.pages.dev)
  - Custom domain removed (was conflicting with Worker routes)
  - Only used for GitHub Actions preview deployments

### Token Info
- `.env.local` token `cfut_Fw0L...` = account Aplesi.co@gmail.com (correct)
- Token must have: Account > Workers Scripts > Edit + Zone > Workers Routes > Edit
- Shell env `CLOUDFLARE_API_TOKEN` may override — use `grep` to get correct token

### Deploy Script
- `npm run deploy` — builds and deploys Worker
- GitHub Actions: `.github/workflows/deploy.yml` — uses `wrangler deploy`

### Key Fix History
- **2026-07-15**: Fixed `panjangConfig` undefined crash — added fallback `|| panjangMap['sedang']`
- **2026-07-15**: Fixed `toneMap` undefined — added fallback `|| toneMap['informatif']`
- **2026-07-15**: Created standalone Worker `aplesi-worker` to avoid Pages/Worker conflict
- **2026-07-15**: Removed custom domains from Pages project
- **2026-07-15**: Fixed image generation — SDXL down, switched to FLUX-1 Schnell model
- **2026-07-15**: Fixed image storage — CF Images 403, now saves to GitHub via API

### AI Image Generation (2026-07-15)
- **Model**: `@cf/black-forest-labs/flux-1-schnell` (FLUX-1) — replaces SDXL which is down
- **Storage**: GitHub API → `public/images/artikel/[slug]-[timestamp].png`
- **Fallback**: `/images/og-default.png` if both AI and GitHub fail
- **Note**: Image URL returns 404 until next deploy (GitHub Actions auto-triggers)
- **CF Images**: Upload fails with 403 (token lacks Images permission) — not used

### Groq API Key Management (2026-07-15)
- **Dynamic add/delete**: `/admin/settings` → section "Kelola API Key"
- **API endpoint**: `app/api/groq/keys/route.ts` — GET/POST/DELETE
- **Storage**: Cloudflare KV (`settings:GROQ_API_KEY`, `settings:GROQ_API_KEY_2`, ...)
- **Max keys**: 10 (was hardcoded 5, now dynamic)
- **Pool**: `lib/ai/groq-pool.ts` — reads from KV, `clearPoolCache()` after add/delete

## P2 Improvement Progress

### Task #1: Riwayat Generate (P2-D) ✅
- [x] Migration SQL: `migrations/0003_generate_log.sql`
- [x] Helper CRUD: `lib/db/generate-log.ts`
- [x] API logging: `app/api/generate/artikel/route.ts`
- [x] API query: `app/api/generate/log/route.ts`
- [x] Admin UI: `app/admin/(dashboard)/riwayat/page.tsx`
- [x] Navigation: `config/navigation.ts`

### Task #2: Preview Sebelum Publish (P2-D) ✅
- [x] Toggle Preview/Markdown di generate page
- [x] Render HTML via `marked`

### Task #3: Strip-fence + Retry JSON (P2-A) ✅
- [x] `stripJsonFence()` + `safeParseGroqJson<T>()`

### Task #4: Fallback Image Prompt (P2-B) ✅
- [x] `buildFallbackImagePrompt(judul, kategori)`

### Task #5: Auto-internal-linking (P2-C) ✅
- [x] `appendInternalLinks()` di groq.ts

### Task #6: Reading Time Frontmatter (P2-C) ✅
- [x] Auto-calculate di publish route

### Task #7: Retry min-words (P2-A) ✅
- [x] `countWords()` + retry logic

### News Crawler Audit (2026-07-15) ✅
- **Status**: SEMUA komponen sudah ada & berfungsi
- **Fix**: Tambah `/news` ke `navPublic` di `config/navigation.ts`
- **Arsitektur**: RSS → scrape → filter kata kunci → AI rewrite → simpan D1 → tampilkan di `/news`
- **Cron**: `generate-berita.yml` tiap 3 jam → scrape + generate recap artikel 800-1200 kata
- **Key files**:
  - `lib/news/sources.ts` — 10 sumber RSS (4 ID + 6 EN)
  - `lib/news/rss-parser.ts` — Parser RSS/Atom ringan + extract imageUrl
  - `lib/news/scraper.ts` — Scrape + filter + dedup + rewrite + hybrid image pipeline
  - `lib/db/berita.ts` — CRUD D1 (termasuk `gambar_url`)
  - `lib/ai/groq.ts` — `rewriteBeritaBatch()` + `generateArtikelDariBerita()` + `buildFallbackImagePrompt()`
  - `lib/ai/cloudflare-image.ts` — `downloadDanSimpanGambar()` + `generateGambarDanSimpan()`
  - `app/(public)/news/page.tsx` — Halaman publik (dengan gambar)
  - `app/api/news/route.ts` — API endpoint
  - `scripts/generate-berita-artikel.mjs` — Cron script (duplicated code, works independently)
- **Note**: Script `.mjs` menduplikasi logic dari `lib/news/` karena tidak bisa import `.ts` langsung

### News Hybrid Image Pipeline (2026-07-15) ✅
- **Arsitektur**: Opsi C (Hybrid)
  - Ada imageUrl dari RSS → download & simpan ke GitHub (0 AI call)
  - Tidak ada imageUrl → generate via FLUX-1 dari judul (2 AI call)
- **Migration**: `migrations/0004_berita_gambar_url.sql` — ALTER TABLE berita ADD COLUMN gambar_url
- **Background processing**: `prosesGambarBerita()` di scraper.ts — max 5 berita/batch, non-blocking
- **Image storage**: `/images/news/` di GitHub repo (sama seperti artikel)
- **RSS extract priority**: media:content > media:thumbnail > enclosure > img in description
- **Deploy**: Token Aplesi.co@gmail.com (CF_API_TOKEN di .env.local), BUKAN shell env CLOUDFLARE_API_TOKEN

### Cron Scripts Fix (2026-07-15) ✅
- **`scripts/generate-berita-artikel.mjs`**: SDXL → FLUX-1, +extractImageUrl, +uploadToGithub, +downloadDanSimpanGambar, hybrid pipeline
- **`scripts/auto-post.mjs`**: SDXL → FLUX-1, +uploadToGithub, GitHub-first image storage
- **Kuota**: 50 gambar/hari (FLUX-1: num_steps=4, ~2500 gambar/bulan gratis)
- **Image model**: `@cf/black-forest-labs/flux-1-schnell` (replaces dead SDXL)

### News Prioritas Indonesia (2026-07-15) ✅
- **Target**: 30 berita/hari (20 ID + 10 EN)
- **Alokasi per siklus** (tiap 3 jam): 70% Indonesia, 30% internasional
- **KV Counter**: `berita:counter:id:[tanggal]` & `berita:counter:en:[tanggal]` (auto-reset harian)
- **Kata kunci split**: `KATA_KUNCI_ID` (24 kata) + `KATA_KUNCI_EN` (20 kata) — sinkron dengan `scraper.ts`
- **Config**: `MAKS_ID_PER_HARI=20`, `MAKS_EN_PER_HARI=10`, `MAKS_PER_SIKLUS=5`
- **Files changed**:
  - `scripts/generate-berita-artikel.mjs` — split kata kunci + alokasi + counter
  - `lib/news/scraper.ts` — split kata kunci + param `asal`
  - `.github/workflows/generate-berita.yml` — commit path `public/images/news/`

## Key Architecture Notes
- D1 via REST API (lib/db/d1.ts), bukan binding langsung
- Generate pipeline: Groq → safeParseGroqJson → appendInternalLinks → optional CF AI image → publish
- 128 artikel sudah published
- Admin sidebar: config/navigation.ts → navAdmin array
- OpenNext build output: `.open-next/` (assets + server-functions + worker.js)
