# MIMO Context — Aplesi Enterprise

## P2 Improvement Progress

### Task #1: Riwayat Generate (P2-D) ✅
- [x] Migration SQL: `migrations/0003_generate_log.sql` — tabel `generate_log`
- [x] Helper CRUD: `lib/db/generate-log.ts` — createGenerateLog, markLogSuccess/Error/Partial, getGenerateLogs, getGenerateStats
- [x] API logging: `app/api/generate/artikel/route.ts` — setiap generate otomatis log ke D1
- [x] API query: `app/api/generate/log/route.ts` — GET logs + stats
- [x] Admin UI: `app/admin/(dashboard)/riwayat/page.tsx` — tabel + filter + stats + pagination
- [x] Navigation: `config/navigation.ts` — tambah "Riwayat Generate" di sidebar

### Task #2: Preview Sebelum Publish (P2-D) ✅
- [x] Toggle Preview/Markdown di `app/admin/(dashboard)/generate/page.tsx`
- [x] Render HTML via `marked` + `dangerouslySetInnerHTML`
- [x] Safety net editorial — admin bisa review konten sebelum publish

### Task #3: Strip-fence + Retry JSON (P2-A) ✅
- [x] `stripJsonFence()` — regex strip ```json ... ``` fence
- [x] `safeParseGroqJson<T>()` — strip dulu, parse, fallback ke raw
- [x] Semua 4 `JSON.parse(raw)` di groq.ts diganti dengan typed generics

### Task #4: Fallback Image Prompt (P2-B) ✅
- [x] `buildFallbackImagePrompt(judul, kategori)` — deterministik, zero LLM cost
- [x] Map kategori → scene spesifik (Pembenihan→hatchery, Pakan→feeding, dll)
- [x] Kedua fallback di `generateArtikel` + `generateArtikelDariBerita` sudah updated

### Task #5: Auto-internal-linking (P2-C) ✅
- [x] `appendInternalLinks()` di groq.ts — query D1 by kategori
- [x] Sisipkan "Baca Juga" section di akhir konten dengan 3 artikel terkait
- [x] Zero LLM cost, zero risk hallucinated slugs

### Task #6: Reading Time Frontmatter (P2-C) ✅
- [x] Auto-calculate di publish route: `Math.ceil(wordCount / 200)`
- [x] Ditambahkan ke frontmatter `.md` sebagai `waktu_baca`
- [x] D1 sudah auto-calculate via `insertArtikel()`

### Task #7: Retry min-words (P2-A) ✅
- [x] `countWords()` helper — strip markdown syntax sebelum hitung kata
- [x] Retry logic di `generateArtikel` — kalau < minWords, retry dengan prompt perluas (max 1x)
- [x] Retry logic di `generateArtikelDariBerita` — kalau < 800 kata, retry (max 1x)
- [x] Pakai hasil retry hanya kalau lebih panjang (safety: jangan downgrade)
- [x] Console warning untuk monitoring: `[Groq] Konten terlalu pendek: X/Y kata. Retry...`

## Files Changed (P2)
- `migrations/0003_generate_log.sql` — NEW
- `lib/db/generate-log.ts` — NEW
- `lib/ai/groq.ts` — MODIFIED (strip-fence, fallback prompt, internal links)
- `app/api/generate/artikel/route.ts` — MODIFIED (logging)
- `app/api/generate/artikel/publish/route.ts` — MODIFIED (reading time)
- `app/api/generate/log/route.ts` — NEW
- `app/admin/(dashboard)/riwayat/page.tsx` — NEW
- `app/admin/(dashboard)/generate/page.tsx` — MODIFIED (preview toggle)
- `config/navigation.ts` — MODIFIED (sidebar nav)
- `.mimo-context.md` — NEW

## Key Architecture Notes
- D1 via REST API (lib/db/d1.ts), bukan binding langsung
- Generate pipeline: Groq → safeParseGroqJson → appendInternalLinks → optional CF AI image → publish
- `response_format: { type: 'json_object' }` sudah dipakai di Groq
- 128 artikel sudah published
- Admin sidebar: config/navigation.ts → navAdmin array
