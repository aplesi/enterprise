# MIMO Context — Aplesi Enterprise

## Deploy Status: ✅ BERHASIL (2026-07-15)

### Deploy Configuration
- **Build**: `opennextjs-cloudflare build` (OpenNext + Next.js 16.2.10 + Turbopack)
- **Deploy**: `wrangler deploy` (Worker with assets binding — handles static + server functions)
- **Worker Name**: `aplesi-worker` (standalone Worker, NOT the Pages project `enterprise`)
- **URL Production**: https://aplesi.my.id (custom domain via Worker routes)
- **URL Preview**: https://enterprise-279.pages.dev (Pages project, static only)
- **Account**: Aplesi.co@gmail.com (Account ID: 3564a2d3d75a9c4794613b8fdeb25359)

### Architecture (IMPORTANT!)
- **Worker `aplesi-worker`** = serves everything (static assets + SSR + API routes)
  - Routes: `aplesi.my.id/*`, `www.aplesi.my.id/*`
  - Assets binding: `.open-next/assets`
  - Bindings: D1 (`aplesi-db`), KV (`aplesi_kv`), Self-reference (`aplesi-worker`)
- **Pages project `enterprise`** = preview only (enterprise-279.pages.dev)
  - Custom domain removed (was conflicting with Worker routes)
  - Only used for GitHub Actions preview deployments

### Token Info
- `.env.local` token `cfut_Fw0L...` = account Aplesi.co@gmail.com (correct)
- Token must have: Account > Workers Scripts > Edit + Zone > Workers Routes > Edit
- Shell env `CLOUDFLARE_API_TOKEN` may override — use `grep` to get correct token

### Deploy Script
- `npm run deploy` — builds and deploys Worker
- GitHub Actions: `.github/workflows/deploy.yml` — uses `wrangler deploy`

### Key Fix History
- **2026-07-15**: Fixed `panjangConfig` undefined crash — added fallback `|| panjangMap['sedang']`
- **2026-07-15**: Fixed `toneMap` undefined — added fallback `|| toneMap['informatif']`
- **2026-07-15**: Created standalone Worker `aplesi-worker` to avoid Pages/Worker conflict
- **2026-07-15**: Removed custom domains from Pages project

## P2 Improvement Progress

### Task #1: Riwayat Generate (P2-D) ✅
- [x] Migration SQL: `migrations/0003_generate_log.sql`
- [x] Helper CRUD: `lib/db/generate-log.ts`
- [x] API logging: `app/api/generate/artikel/route.ts`
- [x] API query: `app/api/generate/log/route.ts`
- [x] Admin UI: `app/admin/(dashboard)/riwayat/page.tsx`
- [x] Navigation: `config/navigation.ts`

### Task #2: Preview Sebelum Publish (P2-D) ✅
- [x] Toggle Preview/Markdown di generate page
- [x] Render HTML via `marked`

### Task #3: Strip-fence + Retry JSON (P2-A) ✅
- [x] `stripJsonFence()` + `safeParseGroqJson<T>()`

### Task #4: Fallback Image Prompt (P2-B) ✅
- [x] `buildFallbackImagePrompt(judul, kategori)`

### Task #5: Auto-internal-linking (P2-C) ✅
- [x] `appendInternalLinks()` di groq.ts

### Task #6: Reading Time Frontmatter (P2-C) ✅
- [x] Auto-calculate di publish route

### Task #7: Retry min-words (P2-A) ✅
- [x] `countWords()` + retry logic

## Key Architecture Notes
- D1 via REST API (lib/db/d1.ts), bukan binding langsung
- Generate pipeline: Groq → safeParseGroqJson → appendInternalLinks → optional CF AI image → publish
- 128 artikel sudah published
- Admin sidebar: config/navigation.ts → navAdmin array
- OpenNext build output: `.open-next/` (assets + server-functions + worker.js)
